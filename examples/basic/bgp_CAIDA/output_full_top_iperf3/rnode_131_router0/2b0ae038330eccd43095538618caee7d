log "/var/log/bird.log" all;
router id 10.0.0.5;
ipv4 table t_direct;
protocol device {
}
protocol kernel {
    ipv4 {
        import all;
        export all;
    };
    learn;
}
protocol direct local_nets {
    ipv4 {
        table t_direct;
        import all;
    };

    interface "net0";

}
define LOCAL_COMM = (131, 0, 0);
define CUSTOMER_COMM = (131, 1, 0);
define PEER_COMM = (131, 2, 0);
define PROVIDER_COMM = (131, 3, 0);
protocol bfd  {
    interface "net0" {
        min rx interval 100 ms;
        min tx interval 100 ms;
        multiplier 3;
    };

    interface "ix100" {
        min rx interval 100 ms;
        min tx interval 100 ms;
        multiplier 3;
    };

}
ipv4 table t_bgp;
protocol pipe {
    table t_bgp;
    peer table master4;
    import all;
    export all;
}
protocol pipe {
    table t_direct;
    peer table t_bgp;
    import all;
    export filter { bgp_large_community.add(LOCAL_COMM); bgp_local_pref = 40; accept; };
}
protocol bgp p_as132 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PEER_COMM);
            bgp_local_pref = 20;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.132 as 132;
    grpc transport yes;
    bfd yes;
}
protocol bgp p_as128 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PEER_COMM);
            bgp_local_pref = 20;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.128 as 128;
    grpc transport yes;
    bfd yes;
}
protocol bgp p_as129 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PEER_COMM);
            bgp_local_pref = 20;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.129 as 129;
    grpc transport yes;
    bfd yes;
}
protocol bgp p_as133 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PEER_COMM);
            bgp_local_pref = 20;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.133 as 133;
    grpc transport yes;
    bfd yes;
}
protocol bgp p_as130 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PEER_COMM);
            bgp_local_pref = 20;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.130 as 130;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as145 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.145 as 145;
    grpc transport yes;
    bfd yes;
}
protocol bgp c_as40 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.40 as 40;
    grpc transport yes;
    bfd yes;
}
protocol bgp c_as50 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.50 as 50;
    grpc transport yes;
    bfd yes;
}
protocol bgp c_as60 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.60 as 60;
    grpc transport yes;
    bfd yes;
}
protocol bgp c_as70 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.70 as 70;
    grpc transport yes;
    bfd yes;
}
protocol bgp c_as80 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.80 as 80;
    grpc transport yes;
    bfd yes;
}
protocol bgp c_as90 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.131 as 131;
    neighbor 10.100.0.90 as 90;
    grpc transport yes;
    bfd yes;
}

