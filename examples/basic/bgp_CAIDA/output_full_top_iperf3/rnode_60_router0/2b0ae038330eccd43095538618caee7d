log "/var/log/bird.log" all;
router id 10.0.0.27;
ipv4 table t_direct;
protocol device {
}
protocol kernel {
    ipv4 {
        import all;
        export all;
    };
    learn;
}
protocol direct local_nets {
    ipv4 {
        table t_direct;
        import all;
    };

    interface "net0";

}
define LOCAL_COMM = (60, 0, 0);
define CUSTOMER_COMM = (60, 1, 0);
define PEER_COMM = (60, 2, 0);
define PROVIDER_COMM = (60, 3, 0);
protocol bfd  {
    interface "net0" {
        min rx interval 100 ms;
        min tx interval 100 ms;
        multiplier 3;
    };

    interface "ix100" {
        min rx interval 100 ms;
        min tx interval 100 ms;
        multiplier 3;
    };

    interface "ix103" {
        min rx interval 100 ms;
        min tx interval 100 ms;
        multiplier 3;
    };

}
ipv4 table t_bgp;
protocol pipe {
    table t_bgp;
    peer table master4;
    import all;
    export all;
}
protocol pipe {
    table t_direct;
    peer table t_bgp;
    import all;
    export filter { bgp_large_community.add(LOCAL_COMM); bgp_local_pref = 40; accept; };
}
protocol bgp u_as131 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PROVIDER_COMM);
            bgp_local_pref = 10;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.131 as 131;
    grpc transport yes;
    bfd yes;
}
protocol bgp u_as132 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PROVIDER_COMM);
            bgp_local_pref = 10;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.132 as 132;
    grpc transport yes;
    bfd yes;
}
protocol bgp u_as128 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PROVIDER_COMM);
            bgp_local_pref = 10;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.128 as 128;
    grpc transport yes;
    bfd yes;
}
protocol bgp u_as127 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PROVIDER_COMM);
            bgp_local_pref = 10;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.127 as 127;
    grpc transport yes;
    bfd yes;
}
protocol bgp u_as130 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PROVIDER_COMM);
            bgp_local_pref = 10;
            accept;
        };
        export where bgp_large_community ~ [LOCAL_COMM, CUSTOMER_COMM];
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.130 as 130;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as64 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.64 as 64;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as61 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.103.0.60 as 60;
    neighbor 10.103.0.61 as 61;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as40 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PROVIDER_COMM);
            bgp_local_pref = 10;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.40 as 40;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as50 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(PROVIDER_COMM);
            bgp_local_pref = 10;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.50 as 50;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as70 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.70 as 70;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as80 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.80 as 80;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as90 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.90 as 90;
    grpc transport yes;
    bfd yes;
}
protocol bgp x_as156 {
    ipv4 {
        table t_bgp;
        import filter {
            bgp_large_community.add(CUSTOMER_COMM);
            bgp_local_pref = 30;
            accept;
        };
        export all;
        next hop self;
    };
    local 10.100.0.60 as 60;
    neighbor 10.100.0.156 as 156;
    grpc transport yes;
    bfd yes;
}

